import React from 'react';

// Functional component named Dash
const Dash = () => {
    return (
        // A div element with specific height, width, background color, and top margin
        <div className='h-[6px] w-[70px] bg-accent mt-10'>
            
        </div>
    );
}

// Exporting the Dash component as the default export
export default Dash;
